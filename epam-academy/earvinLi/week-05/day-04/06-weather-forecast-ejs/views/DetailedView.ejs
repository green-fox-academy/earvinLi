<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link href="../static/style.css" rel="stylesheet" type="text/css"/>
    <title>Todos</title>
  </head>
  <body>
    <div class='datailedViewContainer'>
      <% weathers.map(weather => { %>
          <div class='weatherCardContainer'>
            <div class='weatherIcon'><%= weather.icon %></div>
            <div class='weatherTemp'><%= weather.temp %>&deg;C</div>
            <div class='weatherMessage'><%= weather.message %></div>
          </div>
      <% }) %>
    </div>
  </body>
</html>
